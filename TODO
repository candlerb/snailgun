Work out why programs which interact on stdin (e.g. script/console) cause
the process to stop - need 'fg' to continue

Replacements for script/console, script/runner etc which choose the correct
socket and invoke commands/xxx in fruby

frake to choose the correct socket by duplicating logic from the
:environment task. Could also have a separate forking process for frake

Make it work with autotest (from ZenTest)

Turn into a gem and/or plugin
